<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="action_filestore_cleanup" model="ir.actions.server">
        <field name="name">Cleanup the filestore</field>
        <field name="model_id" ref="base.model_ir_attachment" />
        <field name="state">code</field>
        <field name="code">
count_deleted = model._action_filestore_cleanup()
# TODO: Inform about the total file size.
action = {
    'type': 'ir.actions.client',
    'tag': 'display_notification',
    'params': {
        'title': "title",
        'message': "{} files were deleted.".format(count_deleted),
        'sticky': True,
    }
}
        </field>
    </record>
    <menuitem
        id="menu_filestore_cleanup"
        name="Cleanup the filestore"
        action="action_filestore_cleanup"
        parent="base.next_id_9"
    />
</odoo>
<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="action_filestore_cleanup" model="ir.actions.server">
        <field name="name">Cleanup the filestore</field>
        <field name="model_id" ref="base.model_ir_attachment" />
        <field name="state">code</field>
        <field name="code">
count_deleted = model._action_filestore_cleanup()
# TODO: Inform about the total file size.
action = {
    'type': 'ir.actions.client',
    'tag': 'display_notification',
    'params': {
        'title': "title",
        'message': "{} files were deleted.".format(count_deleted),
        'sticky': True,
    }
}
        </field>
    </record>
    <menuitem
        id="menu_filestore_cleanup"
        name="Cleanup the filestore"
        action="action_filestore_cleanup"
        parent="base.next_id_9"
    />
</odoo>
