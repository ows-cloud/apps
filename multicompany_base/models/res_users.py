from odoo import api, fields, models


# class Users(models.Model):
#     _inherit = 'res.users'

#     replace_record_id = fields.Many2one('res.users', string='Replace Record')
    
#     _sql_constraints = [('replace_res_users_uniq', 'unique(replace_record_id, company_id)', 'Replace Record must be unique per company!')]
